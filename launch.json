{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "clang++ - Build and debug active file",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}", // Path to your executable
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": false, // Set to true if you want a separate console window
            "MIMode": "lldb", // Use lldb for macOS/Clang
            "preLaunchTask": "C/C++: clang++ build active file" // Runs your build task before debugging
        }
    ]
}
